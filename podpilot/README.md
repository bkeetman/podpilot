# PodPilot

PodPilot brings real Kubernetes operations to Raycast. Quickly browse contexts and namespaces, inspect pods and deployments, stream logs, trigger rollouts, and jump into shells — all powered by your existing kubectl configuration.